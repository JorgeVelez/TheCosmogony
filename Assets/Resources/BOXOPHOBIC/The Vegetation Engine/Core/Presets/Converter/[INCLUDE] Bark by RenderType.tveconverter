// Disable Features for Bark

if RENDERTYPE_TAG_CONTAINS Opaque

Material SET_SHADER SHADER_DEFAULT_BARK
Material SET_FLOAT _RenderClip 0

endif

if !KEYWORD_ENABLED _ALPHATEST_ON

Material SET_SHADER SHADER_DEFAULT_BARK
Material SET_FLOAT _RenderClip 0

endif
